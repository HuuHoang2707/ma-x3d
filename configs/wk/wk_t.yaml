# Kernel study on the uncropped pipeline: wider in time only (5x3x3)
_base_: ../prep/rp_none.yaml
name: wk_t
description: wider in time only (5x3x3)
model:
  motion_attention: false
  wide_kernel: reparam
  wk_stages: [res2, res3, res4, res5]
  wk_size: 3
  wk_tsize: 5
