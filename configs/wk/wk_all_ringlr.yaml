# Control: reparameterised, but the ring trains at the backbone learning rate. Separates
# the effect of the ring's own learning rate from the effect of widening frozen stages.
_base_: ../win/wa_wk_all.yaml
name: wk_all_ringlr
description: all-stage wide kernels, ring at the backbone learning rate
train:
  ring_lr: backbone
