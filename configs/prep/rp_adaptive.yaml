# Preprocessing variant built from the raw videos; identical training for all.
_base_: ../exp/e19_long_no_kd.yaml
name: rp_adaptive
description: context-preserving crop (zoom capped at 2x, at least half the frame)
data:
  root: dataset/rwf_adaptive
