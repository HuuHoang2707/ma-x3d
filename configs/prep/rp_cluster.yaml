# Preprocessing variant built from the raw videos; identical training for all.
_base_: ../exp/e19_long_no_kd.yaml
name: rp_cluster
description: person-cluster crop (the thesis pipeline), rebuilt from raw
data:
  root: dataset/rwf_cluster
