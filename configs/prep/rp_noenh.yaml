# Preprocessing variant built from the raw videos; identical training for all.
_base_: ../exp/e19_long_no_kd.yaml
name: rp_noenh
description: person-cluster crop without the bilateral filter and CLAHE
data:
  root: dataset/rwf_noenh
