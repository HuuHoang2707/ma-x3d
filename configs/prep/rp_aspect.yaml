# Preprocessing variant built from the raw videos; identical training for all.
# The raw clips are 1280x720: keep that ratio instead of squashing it into a square.
_base_: ../exp/e19_long_no_kd.yaml
name: rp_aspect
description: full frame, aspect preserved with padding
data:
  root: dataset/rwf_aspect
