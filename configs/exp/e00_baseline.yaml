# Current default recipe (configs/ma_x3d.yaml) under grouped 4-fold CV.
_base_: ../ma_x3d.yaml
name: e00_baseline
description: baseline, 16 frames
data:
  protocol: kfold
train:
  log_test_curve: true   # analysis only (selection-bias measurement)
